import { Routes } from '@angular/router';
import { AppComponent } from './app.component';

// No heavy routing yet — single page. Keep routes list for future use.
export const routes: Routes = [
  { path: '', component: AppComponent }
];
